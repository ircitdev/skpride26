# 🌟 Премиум анимации для модальных окон

## Описание

Система стильных премиум анимаций для модальных окон проекта СК ПРАЙД с использованием GSAP.

**Основные возможности:**
- ✨ Стильная анимация открытия модального окна с эффектом масштабирования и fade
- 📝 Плавная анимация текста с разделением на слова и эффектом появления
- 🎴 Последовательная анимация каждого элемента sport-card с stagger эффектом
- 🚪 Плавная анимация закрытия с fade-out эффектом
- ⚡ GPU-ускоренные трансформации для максимальной производительности
- 📱 Полная адаптивность для мобильных устройств
- ♿ Поддержка accessibility (prefers-reduced-motion)

---

## 📁 Файлы

Система состоит из двух файлов:

1. **modal-animations-premium.js** - JavaScript логика анимаций
2. **modal-animations-premium.css** - CSS стили для анимаций

---

## 🎬 Как работают анимации

### Фаза 1: Открытие модального окна (0s - 0.8s)

1. **Backdrop** появляется с blur эффектом
   - Длительность: 0.6s
   - Эффект: плавное появление + backdrop-filter blur(20px)

2. **Контент модального окна** появляется с масштабированием
   - Начальное состояние: opacity: 0, scale: 0.9, y: 30
   - Конечное состояние: opacity: 1, scale: 1, y: 0
   - Длительность: 0.8s
   - Easing: power3.out

3. **Видео-фон** плавно проявляется
   - Длительность: 0.6s

### Фаза 2: Заголовки и статистика (0.5s - 1.2s)

1. **Заголовки** появляются по словам
   - Текст разбивается на отдельные слова
   - Каждое слово анимируется отдельно
   - Эффект: opacity: 0 → 1, y: 30 → 0, rotationX: -20 → 0
   - Длительность: 0.7s
   - Stagger: 0.03s между словами
   - Easing: back.out(1.2) - легкий отскок

2. **Статистика** (560м², 40+ тренеров, 7 лет)
   - Каждый элемент анимируется отдельно
   - Эффект: scale: 0.8 → 1, y: 20 → 0
   - Stagger: 0.1s между элементами

3. **Intro текст** появляется по словам
   - Аналогично заголовкам
   - Более простой эффект без rotationX

### Фаза 3: Карточки (1.0s - 2.5s)

Каждая карточка анимируется последовательно с задержкой 0.15s:

1. **Основная анимация карточки:**
   - Эффект: opacity: 0 → 1, y: 50 → 0, rotationY: 3 → 0, scale: 0.95 → 1
   - Длительность: 0.6s
   - Easing: power2.out

2. **Содержимое карточки** (последовательно):
   - Изображение: scale: 1.2 → 1 (эффект zoom-out)
   - Заголовок (h3): y: 20 → 0
   - Текст (p): opacity: 0 → 1
   - Кнопки: y: 10 → 0 с stagger 0.05s между кнопками

**Общая длительность открытия:** ~2.5 секунды

### Фаза 4: Закрытие модального окна (0s - 0.5s)

Закрытие происходит в обратном порядке, но быстрее:

1. **Карточки** исчезают первыми (в обратном порядке)
   - Эффект: y: -30, scale: 0.9, opacity: 0
   - Stagger: 0.05s

2. **Текст и заголовки** исчезают
   - Эффект: y: -20, opacity: 0

3. **Контент и backdrop** исчезают одновременно
   - Длительность: 0.5s
   - Easing: power2.in (быстрое ускорение)

4. **Сброс состояния** - все элементы возвращаются к начальному состоянию

**Общая длительность закрытия:** 0.5 секунды

---

## ⚙️ Конфигурация

Все параметры анимаций настраиваются в объекте `ANIMATION_CONFIG`:

```javascript
const ANIMATION_CONFIG = {
  // Длительности
  duration: {
    modalOpen: 0.8,           // Открытие модального окна
    modalClose: 0.5,          // Закрытие модального окна
    backdrop: 0.6,            // Backdrop fade
    text: 0.7,                // Анимация текста
    card: 0.6,                // Анимация карточки
  },

  // Задержки
  delay: {
    textAfterModal: 0.3,      // Текст после открытия модалки
    cardsAfterText: 0.2,      // Карточки после текста
    cardStagger: 0.15,        // Между каждой карточкой
    wordStagger: 0.03,        // Между словами в тексте
  },

  // Easing функции
  ease: {
    modalOpen: 'power3.out',       // Плавное замедление
    modalClose: 'power2.in',       // Быстрое ускорение
    text: 'back.out(1.2)',         // Легкий отскок
    card: 'power2.out',            // Стандартное замедление
    backdrop: 'power2.inOut',      // Симметричное
  },

  // Эффекты
  effects: {
    modalScale: 0.9,          // Начальный scale модалки
    textY: 30,                // Сдвиг текста вверх
    cardY: 50,                // Сдвиг карточек вверх
    cardRotation: 3,          // Легкий поворот карточек
  }
};
```

### Как изменить настройки:

1. Откройте файл `modal-animations-premium.js`
2. Найдите объект `ANIMATION_CONFIG` (строка ~21)
3. Измените нужные параметры
4. Сохраните файл

**Примеры:**

```javascript
// Сделать анимацию открытия быстрее
duration: {
  modalOpen: 0.5,  // было 0.8
}

// Увеличить задержку между карточками
delay: {
  cardStagger: 0.25,  // было 0.15
}

// Изменить эффект отскока текста
ease: {
  text: 'back.out(2)',  // было 'back.out(1.2)' - сильнее отскок
}

// Сильнее масштабировать модальное окно при открытии
effects: {
  modalScale: 0.8,  // было 0.9 - начинается с меньшего размера
}
```

---

## 🎯 Поддерживаемые модальные окна

Система автоматически применяет анимации к следующим модальным окнам:

1. **sportModal** - Спортивные направления
2. **kidsModal** - Детские программы
3. **restModal** - Релакс и отдых
4. **eventsModal** - Мероприятия
5. **contactsModal** - Контакты

Каждое модальное окно имеет свою конфигурацию селекторов для точной анимации элементов.

---

## 🔧 API

### Глобальный объект

После загрузки скрипта доступен глобальный объект `window.PrideModalAnimationsPremium`:

```javascript
window.PrideModalAnimationsPremium = {
  init: Function,                      // Функция инициализации
  config: Object,                      // Конфигурация анимаций
  ModalAnimationController: Class,     // Класс контроллера
};
```

### Методы

#### Повторная инициализация

```javascript
window.PrideModalAnimationsPremium.init();
```

#### Доступ к конфигурации

```javascript
console.log(window.PrideModalAnimationsPremium.config);
```

#### Доступ к контроллерам модальных окон

```javascript
// Массив всех контроллеров
window.ModalAnimationControllers
```

### Контроллер модального окна

Каждое модальное окно управляется экземпляром класса `ModalAnimationController`:

```javascript
class ModalAnimationController {
  constructor(modalId, selectors)

  // Методы
  init()                  // Инициализация
  playOpenAnimation()     // Запуск анимации открытия
  playCloseAnimation()    // Запуск анимации закрытия
  hideAllElements()       // Скрыть все элементы
  observeModalState()     // Отслеживание состояния модального окна
  setupCloseHandlers()    // Настройка обработчиков закрытия
}
```

---

## 🎨 CSS классы

### Автоматически добавляемые классы

Скрипт автоматически добавляет следующие классы к модальным окнам:

- `.modal-animating` - модальное окно в процессе открытия
- `.modal-closing` - модальное окно в процессе закрытия

Эти классы можно использовать для дополнительной стилизации:

```css
.sport-modal.modal-animating {
  /* Стили во время анимации открытия */
}

.sport-modal.modal-closing {
  /* Стили во время анимации закрытия */
}
```

### Классы для анимированных элементов

- `.modal-backdrop-premium` - backdrop с blur эффектом
- `.word-wrapper` - обертка для анимированного слова
- `.word-animated` - анимированное слово

---

## 📱 Адаптивность

### Мобильные устройства (< 768px)

На мобильных устройствах применяются упрощенные анимации:

- Уменьшенный blur эффект (10px вместо 20px)
- Отключены сложные 3D трансформации
- Упрощены hover эффекты
- Уменьшены padding и gap

### Accessibility

При включенной настройке `prefers-reduced-motion` все анимации отключаются:

```css
@media (prefers-reduced-motion: reduce) {
  /* Все анимации отключены */
}
```

---

## 🔍 Отладка

### Консольные сообщения

Скрипт выводит подробные сообщения в консоль:

```
🎬 Инициализация премиум анимаций модальных окон...
✅ Контроллер анимации для sportModal инициализирован
🎬 Начало анимации открытия: sportModal
✅ Анимация открытия завершена: sportModal
🎬 Начало анимации закрытия: sportModal
✅ Анимация закрытия завершена: sportModal
```

### Проверка загрузки

```javascript
// Проверить что скрипт загружен
if (window.PrideModalAnimationsPremium) {
  console.log('✅ Премиум анимации загружены');
} else {
  console.log('❌ Премиум анимации не загружены');
}

// Проверить контроллеры
console.log('Контроллеры:', window.ModalAnimationControllers);
```

---

## ⚡ Производительность

### Оптимизация

Система использует следующие техники оптимизации:

1. **GPU acceleration** - transform: translateZ(0)
2. **will-change** для анимируемых свойств
3. **backface-visibility: hidden** для предотвращения flickering
4. **gsap.defaults({ overwrite: 'auto' })** для автоматической очистки конфликтующих анимаций

### Рекомендации

- Не открывайте несколько модальных окон одновременно
- Дождитесь завершения анимации перед закрытием
- На слабых устройствах рассмотрите уменьшение `duration` параметров

---

## 🐛 Известные проблемы

### Проблема: Анимация не запускается

**Решение:**
1. Проверьте что GSAP загружен:
   ```javascript
   console.log(typeof gsap);  // должно быть 'object'
   ```
2. Проверьте что модальное окно существует в DOM
3. Проверьте консоль на наличие ошибок

### Проблема: Карточки не анимируются

**Решение:**
1. Убедитесь что элементы имеют правильные классы (`.sport-card`, `.rest-card` и т.д.)
2. Проверьте что селекторы в конфигурации соответствуют вашей разметке

### Проблема: Текст не разбивается на слова

**Решение:**
- Убедитесь что текст не содержит HTML тегов внутри
- Проверьте что элемент имеет `textContent`

---

## 🔄 Обновление с предыдущей версии

Если вы используете `modal-animations-enhanced.js`, вы можете безопасно переключиться на премиум версию:

1. Закомментируйте старые подключения в `index.html`:
   ```html
   <!-- <link rel="stylesheet" type="text/css" href="modal-animations-enhanced.css"> -->
   <!-- <script src="./modal-animations-enhanced.js"></script> -->
   ```

2. Добавьте новые подключения:
   ```html
   <link rel="stylesheet" type="text/css" href="modal-animations-premium.css">
   <script src="./modal-animations-premium.js"></script>
   ```

3. Очистите кеш браузера (Ctrl+F5)

**Различия:**

| Функция | Enhanced | Premium |
|---------|----------|---------|
| Анимация backdrop | ✓ | ✓ |
| Анимация контента | ✓ | ✓ |
| Разделение текста на слова | ✗ | ✓ |
| Последовательная анимация элементов карточек | ✗ | ✓ |
| Настраиваемая конфигурация | ограничено | ✓ |
| 3D трансформации | ✗ | ✓ |
| Детальная отладка | ✗ | ✓ |

---

## 💡 Примеры использования

### Программное открытие модального окна

```javascript
// Открыть модальное окно
const sportModal = document.getElementById('sportModal');
sportModal.classList.add('active');

// Анимация запустится автоматически
```

### Программное закрытие модального окна

```javascript
// Найти контроллер
const controller = window.ModalAnimationControllers.find(
  c => c.modal.id === 'sportModal'
);

// Закрыть с анимацией
if (controller) {
  controller.playCloseAnimation();
}
```

### Изменение анимации в реальном времени

```javascript
// Изменить конфигурацию
window.PrideModalAnimationsPremium.config.duration.modalOpen = 0.5;
window.PrideModalAnimationsPremium.config.delay.cardStagger = 0.25;

// Переинициализировать
window.PrideModalAnimationsPremium.init();
```

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь что все файлы подключены в правильном порядке
3. Проверьте что GSAP загружен до премиум анимаций
4. Очистите кеш браузера

---

## 📜 Лицензия

Часть проекта СК ПРАЙД. Все права защищены.

---

## 🎉 Changelog

### v1.0.0 (2025-01-08)
- ✨ Первый релиз премиум анимаций
- 🎬 Трехфазная система анимации открытия
- 📝 Разделение текста на слова
- 🎴 Последовательная анимация карточек
- 🚪 Плавное закрытие модального окна
- 📱 Полная адаптивность
- ♿ Поддержка accessibility
- ⚡ GPU-ускорение
- 🔧 Гибкая конфигурация
- 🐛 Подробная отладка

---

**Создано с ❤️ для СК ПРАЙД**
