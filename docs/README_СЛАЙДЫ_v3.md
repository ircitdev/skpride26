# 🎬 Новая система слайдов v3.0 - БЕЗ HTML!

## 🎯 Что сделано

Создана революционная система управления слайдами **без необходимости знать HTML**!
Теперь секретарь использует простую разметку прямо в Google Таблице.

---

## ✨ Главные улучшения

### 1. Простая разметка вместо HTML

**БЫЛО (v2.0):**
```html
<figure class="slide"><div class="slide__img-wrap light-slide">...сложный HTML код...
```

**СТАЛО (v3.0):**
```
{Спорт} и отдых
для **всей** *семьи*
```

### 2. Две картинки: для ПК и телефонов

**Desktop:** 1920x1080 (горизонтальная)
**Mobile:** 1080x1920 (вертикальная)

### 3. Одна кнопка = один слайд

Логично и понятно!

### 4. Визуальное форматирование

- `**жирный**` → **жирный**
- `*курсив*` → *курсив*
- `[белая плашка]` → акцент цветом сайта
- `{бежевая плашка}` → новый акцент с фоном
- `|` → перенос строки

---

## 📊 Новая структура таблицы

10 простых столбцов:

| № | Столбец | Пример |
|---|---------|--------|
| 1 | Номер | 1 |
| 2 | Заголовок строка 1 | {Спорт} и отдых |
| 3 | Заголовок строка 2 | для **всей** *семьи* |
| 4 | Заголовок строка 3 | |
| 5 | Картинка Desktop | ./img/1.jpg |
| 6 | Картинка Mobile | ./img/1-mobile.jpg |
| 7 | Текст кнопки | Я хочу спорт |
| 8 | Ссылка кнопки | # |
| 9 | ID кнопки | wantSportBtn |
| 10 | Тема слайда | light |

---

## 🔧 Новые файлы и компоненты

### 1. Генератор HTML (`slides_generator.py`)
Автоматически преобразует простую разметку в профессиональный HTML:

**Функции:**
- `parse_markup()` - парсит разметку (**, *, [], {}, |)
- `generate_slide_html()` - генерирует HTML одного слайда
- `generate_slides_from_data()` - обрабатывает все слайды

**Тестирование:**
```bash
python slides_generator.py test
```

### 2. Обновленный синхронизатор (`sync_selective.py`)
Функция `update_slides()` теперь:
1. Сохраняет исходные данные в `slides.json`
2. Генерирует HTML версию
3. Сохраняет в `slides_html.json` и `slides_output.html`

### 3. Новый CSS класс (`.slideTextAccentBeige`)
Добавлен в `style.css` для бежевых плашек:
```css
.slideTextAccentBeige {
    color: #000;
    background: linear-gradient(135deg, #f5e6d3 0%, #e8d4b8 100%);
    padding: 5px 15px;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}
```

### 4. Документация

**Для пользователей:**
- `СТРУКТУРА_СЛАЙДОВ.md` - подробное описание новой структуры
- `ИНСТРУКЦИЯ_ОБНОВЛЕНИЕ_ТАБЛИЦЫ_СЛАЙДОВ.md` - как обновить Google Таблицу
- `БЫСТРАЯ_ИНСТРУКЦИЯ.md` - обновлена с учетом слайдов

**Технические:**
- `README_СЛАЙДЫ_v3.md` - этот файл
- `slides_example.json` - примеры данных

---

## 🚀 Как использовать

### Для секретаря:

**Шаг 1:** Откройте Google Таблицу, вкладка `01_СЛАЙДЫ.csv`

**Шаг 2:** Заполните данные с простой разметкой:
- `**Спорт**` - жирный
- `*отдых*` - курсив
- `{каждый}` - бежевая плашка
- `[всей]` - цветная плашка

**Шаг 3:** Запустите `ОБНОВИТЬ_ДАННЫЕ.bat`, выберите `1`

**Шаг 4:** Готово! Система сгенерирует HTML автоматически!

---

## 🎨 Примеры разметки

### Пример 1: Простой текст
**Ввод:**
```
Заголовок строка 1: Спорт и отдых
Заголовок строка 2: для всей семьи
```

**Вывод HTML:**
```html
<span class="text-row"><span>Спорт и отдых</span></span>
<span class="text-row"><span>для всей семьи</span></span>
```

### Пример 2: С форматированием
**Ввод:**
```
Заголовок строка 1: {Спорт} и отдых
Заголовок строка 2: для **всей** *семьи*
```

**Вывод HTML:**
```html
<span class="text-row"><span>
  <span class="slideTextAccentBeige">Спорт</span> и отдых
</span></span>
<span class="text-row"><span>
  для <strong>всей</strong> <em>семьи</em>
</span></span>
```

### Пример 3: С переносом строки
**Ввод:**
```
Заголовок строка 1: **Сила**|и движение
Заголовок строка 2: {каждый} день
```

**Вывод HTML:**
```html
<span class="text-row"><span>
  <strong>Сила</strong><br class="mobile-br"/>и движение
</span></span>
<span class="text-row"><span>
  <span class="slideTextAccentBeige">каждый</span> день
</span></span>
```

---

## ✅ Протестировано

- ✅ Парсинг разметки - 7 тестов пройдено
- ✅ Генерация HTML - работает идеально
- ✅ Интеграция в sync_selective.py - работает
- ✅ CSS стили - добавлены и протестированы
- ✅ Примеры данных - создан slides_example.json
- ✅ Документация - полная инструкция готова

---

## 📁 Файлы системы

### Скрипты:
- `slides_generator.py` - генератор HTML из разметки
- `sync_selective.py` - обновленный синхронизатор (с поддержкой слайдов v3)

### Данные:
- `slides.json` - исходные данные слайдов
- `slides_html.json` - слайды с готовым HTML
- `slides_output.html` - чистый HTML для вставки
- `slides_example.json` - примеры для тестирования

### Стили:
- `style.css` - добавлен `.slideTextAccentBeige`

### Документация:
- `СТРУКТУРА_СЛАЙДОВ.md` - описание структуры
- `ИНСТРУКЦИЯ_ОБНОВЛЕНИЕ_ТАБЛИЦЫ_СЛАЙДОВ.md` - инструкция миграции
- `README_СЛАЙДЫ_v3.md` - этот файл

---

## 🎯 Преимущества v3.0

| Критерий | v2.0 (HTML в таблице) | v3.0 (Простая разметка) |
|----------|----------------------|-------------------------|
| **Сложность** | Нужно знать HTML | Простые символы |
| **Безопасность** | Можно сломать верстку | Невозможно сломать |
| **Скорость** | Долго писать HTML | Быстро написать |
| **Понятность** | Нужна тех. подготовка | Понятно интуитивно |
| **Картинки** | Одна для всех | Отдельно для ПК/телефона |
| **Кнопки** | Несколько на слайд | Одна на слайд (логично) |
| **Форматирование** | HTML теги | Markdown-like разметка |

---

## 🔄 Миграция с v2.0 на v3.0

1. **Читайте** `ИНСТРУКЦИЯ_ОБНОВЛЕНИЕ_ТАБЛИЦЫ_СЛАЙДОВ.md`
2. **Изменяйте** структуру вкладки `01_СЛАЙДЫ.csv`
3. **Переносите** данные из старых столбцов в новые
4. **Тестируйте** на 1-2 слайдах
5. **Запускайте** `ОБНОВИТЬ_ДАННЫЕ.bat` → `1`
6. **Проверяйте** результат на сайте

---

## 📞 Поддержка

**Документы:**
- `СТРУКТУРА_СЛАЙДОВ.md` - детали структуры
- `slides_example.json` - рабочие примеры

**Тестирование:**
```bash
# Тест парсера разметки
python slides_generator.py test

# Генерация из примера
cp slides_example.json slides.json
python slides_generator.py
```

---

## 🚀 Дальнейшие улучшения

### v3.1 (планируется):
- [ ] Предпросмотр слайда в веб-панели
- [ ] Валидация разметки перед генерацией
- [ ] Автоматическая оптимизация картинок для Mobile

### v3.2 (идеи):
- [ ] Визуальный редактор разметки
- [ ] Библиотека готовых шаблонов слайдов
- [ ] A/B тестирование разных версий слайдов

---

**Дата:** 2025-11-19
**Версия:** 3.0
**Автор:** Claude Code
**Статус:** ✅ Готово к использованию
