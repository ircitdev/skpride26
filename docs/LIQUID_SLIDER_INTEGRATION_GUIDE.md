# 🌊 Liquid Morphology Slider - Integration Guide

## Что было сделано

Создан новый слайдер на основе Three.js с эффектами liquid morphology из референсного проекта. Slider включает:

✅ Three.js WebGL рендеринг с кастомными шейдерами
✅ Glass morphology effect (жидкое стекло)
✅ GSAP анимации для плавных переходов
✅ Автоплей с прогресс-барами
✅ Навигация (prev/next кнопки, клик по навигации)
✅ Keyboard support (← → Space)
✅ Touch/swipe support для мобильных
✅ Responsive дизайн
✅ Все 10 слайдов из текущего сайта

## Созданные файлы

1. **liquid-slider.js** - основной JavaScript с Three.js и логикой слайдера
2. **liquid-slider.css** - стили для слайдера
3. **liquid-slider-demo.html** - demo страница для тестирования
4. **LIQUID_SLIDER_INTEGRATION_GUIDE.md** - этот файл

## Интеграция в main index.html

### Шаг 1: Добавить CSS в <head>

Найдите строку:
```html
<!-- 🪟 Liquid Glass эффект (ОТКЛЮЧЕНО - ломает верстку) -->
<!-- <link rel="stylesheet" type="text/css" href="glass-navigation.css"> -->
```

Добавьте после неё:
```html
<!-- 🌊 Three.js Liquid Morphology Slider -->
<link rel="stylesheet" type="text/css" href="liquid-slider.css">
```

**Статус: ✅ УЖЕ ДОБАВЛЕНО в строку 151**

### Шаг 2: Добавить script перед </body>

В конце файла перед `</body>` добавьте:
```html
<!-- 🌊 Three.js Liquid Morphology Slider -->
<script type="module" src="liquid-slider.js"></script>
```

**Статус: ✅ УЖЕ ДОБАВЛЕНО в строку 2757**

### Шаг 3: Заменить старый slideshow на liquid slider

Найдите в index.html строку **326**:
```html
<div class="slideshow">
```

И всё до строки **465**:
```html
</div>
```

Замените этот блок (строки 326-465) на:
```html
<!-- 🌊 LIQUID MORPHOLOGY SLIDER -->
<div class="liquid-slider-wrapper">
	<canvas class="liquid-slider-canvas"></canvas>

	<!-- Slide counter -->
	<span class="liquid-slide-number" id="liquidSlideNumber">01</span>
	<span class="liquid-slide-total" id="liquidSlideTotal">10</span>

	<!-- Bottom navigation -->
	<nav class="liquid-slides-nav" id="liquidSlidesNav">
		<!-- Generated by JavaScript -->
	</nav>
</div>

<!-- OLD SLIDESHOW - МОЖЕТЕ УДАЛИТЬ ПОЗЖЕ -->
<!--
[весь старый slideshow код можно закомментировать]
-->
```

## Как протестировать

### Вариант 1: Открыть demo файл
```
Откройте liquid-slider-demo.html в браузере
```

### Вариант 2: После интеграции в index.html
```
1. Откройте index.html в браузере
2. Проверьте, что:
   - Слайдер показывает изображения
   - Liquid glass effect работает при переходах
   - Автоплей работает (каждые 5 секунд)
   - Кнопки ← → работают
   - Прогресс-бары заполняются
   - Touch/swipe работает на мобильных
```

## Настройки

В файле `liquid-slider.js` можно изменить:

```javascript
const SLIDER_CONFIG = {
  transitionDuration: 2.5,     // длительность перехода (секунды)
  autoSlideSpeed: 5000,        // автоплей (миллисекунды)
  currentEffect: "glass"        // эффект: glass, frost, ripple, plasma, timeshift
};
```

## Сохранённые функции

✅ **Навигационные кнопки** - существующие `.slides-nav__button--prev/next` работают
✅ **Все 10 слайдов** - данные взяты из текущих слайдов
✅ **Автоплей** - как в оригинале
✅ **Responsive** - адаптивная верстка
✅ **BackgroundCheck** - будет работать после интеграции

## Что НЕ было перенесено

❌ **Текст и кнопки на слайдах** - сейчас показываются только изображения
   - Если нужен текст, требуется добавить overlay элементы

❌ **Модальные окна** - клики по кнопкам на слайдах
   - Требуется добавить отдельный overlay с кнопками

## Добавление текста на слайды (опционально)

Если нужно вернуть текст и кнопки:

1. Добавьте в `liquid-slider-wrapper`:
```html
<div class="slide-overlay" id="slideOverlay">
	<!-- Контент текущего слайда -->
</div>
```

2. В `liquid-slider.js` добавьте данные слайдов:
```javascript
const slides = [
  {
    title: "Спорт и отдых для всей семьи",
    image: "./img/1.jpg",
    caption: "<h2>Спорт<br>и отдых</h2>",
    buttons: [...]
  },
  //...
];
```

3. Обновляйте overlay при смене слайда

## Troubleshooting

**Проблема**: Слайдер не показывается
- **Решение**: Проверьте консоль браузера (F12). Убедитесь что:
  - `liquid-slider.js` загрузился (должен быть type="module")
  - Изображения доступны (./img/1.jpg и т.д.)
  - Three.js загрузился (используется ESM импорт)

**Проблема**: Изображения не загружаются
- **Решение**: Проверьте пути к изображениям в `liquid-slider.js`:
```javascript
const slides = [
  { image: "./img/1.jpg" },  // убедитесь что путь правильный
  // ...
];
```

**Проблема**: Навигация не работает
- **Решение**: Проверьте что кнопки `.slides-nav__button--prev/next` существуют в HTML

## Следующие шаги

1. ✅ Тестируйте liquid-slider-demo.html
2. ⬜ Замените slideshow в index.html (строки 326-465)
3. ⬜ Протестируйте в браузере
4. ⬜ Опционально: добавьте текст и кнопки на слайды
5. ⬜ Опционально: попробуйте другие эффекты (frost, plasma, timeshift)

## Контакты и поддержка

Все файлы находятся в: `K:\scripts\pride\slide7\`

- liquid-slider.js
- liquid-slider.css
- liquid-slider-demo.html
- LIQUID_SLIDER_INTEGRATION_GUIDE.md
