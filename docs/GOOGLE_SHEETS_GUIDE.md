# 📊 Инструкция по работе с контентом в Google Таблицах

> **Для менеджеров СК ПРАЙД**
> Простое редактирование контента сайта без знания программирования

---

## 🎯 Зачем это нужно?

Теперь весь контент сайта (описания услуг, цены, расписания, информация о тренерах) можно редактировать в удобных Google Таблицах, не касаясь кода!

---

## 📁 Что было создано

### Файлы для работы:

1. **export-to-google.ps1** - экспорт данных в CSV для загрузки в Google Таблицы
2. **import-from-google.ps1** - импорт отредактированных данных обратно на сайт
3. **export_to_sheets.py** - Python скрипт экспорта (запускается автоматически)
4. **import_from_sheets.py** - Python скрипт импорта (запускается автоматически)

### Папка с данными:

- **google_sheets_export/** - CSV файлы для Google Таблиц (11 категорий + общий файл)

---

## 🚀 Быстрый старт

### Шаг 1: Экспорт данных в CSV

1. Откройте папку проекта `K:\scripts\pride\slide6`
2. **Двойной клик** по файлу `export-to-google.ps1`
3. Дождитесь завершения (появится папка `google_sheets_export`)

### Шаг 2: Загрузка в Google Таблицы

#### Вариант А: Все данные одним файлом (проще)

1. Откройте [Google Таблицы](https://docs.google.com/spreadsheets/)
2. Создайте новую таблицу
3. **Файл → Импортировать → Загрузить**
4. Выберите файл `google_sheets_export/ВСЕ_ДАННЫЕ.csv`
5. Настройки импорта:
   - **Разделитель**: Табуляция
   - **Кодировка**: UTF-8
   - **Импортировать в**: Новый лист

#### Вариант Б: По категориям (удобнее для работы)

1. Откройте Google Таблицы
2. Создайте новую таблицу
3. Для каждого CSV файла (gym.csv, section.csv и т.д.):
   - **Файл → Импортировать → Загрузить**
   - Выберите файл
   - **Импортировать в**: Вставить новые листы
   - **Разделитель**: Табуляция
4. Переименуйте вкладки в понятные названия:
   - `gym` → Тренажерный зал
   - `section` → Секции
   - `relax` → Зона отдыха
   - `hotel` → Отель
   - и т.д.

### Шаг 3: Редактирование контента

Откройте Google Таблицы и редактируйте данные в колонках:

| Колонка | Что редактировать | Пример |
|---------|------------------|--------|
| **Название** | Название услуги/тренера | "Иванов Сергей" |
| **Подпись** | Короткое описание | "Фитнес-тренер" |
| **Краткое описание** | Текст для карточки (HTML) | "Тренер с опытом 10 лет..." |
| **Полное описание** | Расширенное описание (HTML) | "<b>Программы:</b><br>..." |
| **Расписание** | HTML с расписанием | "Пн, Ср, Пт 10:00-12:00" |
| **Цена** | HTML с ценами | "1 занятие — 500 ₽<br>8 занятий — 3000 ₽" |
| **Иконка** | Путь к иконке | "icons/gym.png" |
| **Обложка** | Путь к обложке | "covers/gym.jpg" |

#### ⚠️ НЕ РЕДАКТИРОВАТЬ:

- **ID** - уникальный идентификатор
- **Родитель** - связь с родительским элементом
- **Уровень** - глубина вложенности
- **Значение** - системный ID

### Шаг 4: Скачивание из Google Таблиц

#### Если использовали вариант А (все данные):

1. **Файл → Скачать → CSV (текущий лист)**
2. Сохраните как `ВСЕ_ДАННЫЕ.csv`
3. Скопируйте в `google_sheets_export/` (замените старый файл)

#### Если использовали вариант Б (по категориям):

Для каждой вкладки:
1. Выберите вкладку
2. **Файл → Скачать → CSV (текущий лист)**
3. Сохраните с правильным именем (gym.csv, section.csv и т.д.)
4. Скопируйте в `google_sheets_export/`

### Шаг 5: Импорт обратно на сайт

1. Откройте папку проекта `K:\scripts\pride\slide6`
2. **Двойной клик** по файлу `import-from-google.ps1`
3. Подтвердите импорт (будет создан бэкап `form.json`)
4. Дождитесь завершения
5. Проверьте сайт локально (скрипт предложит запустить сервер)

---

## 📋 Структура данных

### 11 категорий сайта:

1. **gym** - Тренажерный зал (13 строк: 1 корневая + 2 подкатегории с 10 тренерами)
2. **section** - Секции (74 строки: детские, боевые искусства, танцы и т.д.)
3. **relax** - Зона отдыха (1 строка)
4. **arena** - Ледовая арена (1 строка)
5. **solarium** - Солярий (1 строка)
6. **massage** - Массаж (1 строка)
7. **hotel** - Отель (1 строка)
8. **field** - Футбольное поле (4 строки)
9. **event** - Мероприятия (4 строки)
10. **restaurant** - Ресторан (1 строка)
11. **events** - События (6 строк)

**Всего: 107 строк контента**

### Иерархия (4 уровня):

```
Уровень 0: Главная категория (например, "Тренажерный зал")
  ↓
Уровень 1: Подкатегория (например, "Запись к тренеру")
  ↓
Уровень 2: Элемент (например, "Иванов Сергей")
  ↓
Уровень 3: Под-элемент (если есть)
```

---

## 💡 Советы по редактированию

### HTML в описаниях

Вы можете использовать простые HTML теги:

```html
<b>Жирный текст</b>
<i>Курсив</i>
<br> - перенос строки
<br><br> - пустая строка

Пример:
<b>🎟 Абонементы:</b><br>
8 занятий — 2600 ₽<br>
12 занятий — 3200 ₽<br>
Безлимитный — 4200 ₽
```

### Переводы строк

В CSV переводы строк закодированы как `\n`:
- Если видите `\n` - это перевод строки
- При редактировании используйте `<br>` вместо Enter

### Эмодзи

Эмодзи работают отлично! Используйте для привлечения внимания:
- 🏋️ Тренировки
- 💳 Цены
- 📅 Расписание
- 🎯 Акции
- ⚡ Важно

### Пути к картинкам

Пути относительно корня сайта:
- `icons/gym.png` - иконки
- `covers/gym.jpg` - обложки
- `https://pride34.ru/...` - можно использовать полные URL

---

## 🔄 Рабочий процесс

### Регулярное обновление контента:

```mermaid
1. Экспорт → 2. Загрузка в Google Таблицы → 3. Редактирование →
4. Скачивание CSV → 5. Импорт → 6. Проверка → 7. Публикация
```

### Как часто делать экспорт?

- **Первый раз**: полный экспорт всех данных
- **При добавлении нового контента**: новый экспорт
- **При обычном редактировании**: используйте существующую таблицу

---

## ⚠️ Важные предупреждения

### 🚫 Что НЕЛЬЗЯ делать:

1. ❌ Удалять колонки ID, Родитель, Уровень, Значение
2. ❌ Менять порядок строк (это нарушит иерархию)
3. ❌ Редактировать колонку "Значение" (системные ID)
4. ❌ Удалять строки без понимания структуры

### ✅ Что МОЖНО делать:

1. ✅ Редактировать тексты в колонках: Название, Подпись, Описания
2. ✅ Менять цены и расписания
3. ✅ Добавлять HTML форматирование
4. ✅ Менять пути к картинкам
5. ✅ Добавлять эмодзи

### 🛡️ Безопасность:

- При импорте **автоматически создается бэкап** `form.json.backup.ДАТА_ВРЕМЯ`
- Если что-то пошло не так - восстановите из бэкапа
- Всегда **проверяйте сайт локально** перед публикацией

---

## 🆘 Решение проблем

### Проблема: "Python не найден"

**Решение**: Установите Python с https://www.python.org/downloads/

### Проблема: "Кракозябры вместо русских букв"

**Решение**:
1. При импорте в Google Таблицы выберите кодировку **UTF-8**
2. При скачивании - тоже UTF-8

### Проблема: "Данные импортировались неправильно"

**Решение**:
1. Проверьте, что разделитель - **Табуляция** (не запятая!)
2. Восстановите из бэкапа: переименуйте `form.json.backup.ДАТА_ВРЕМЯ` в `form.json`

### Проблема: "Сайт не работает после импорта"

**Решение**:
1. Проверьте консоль браузера (F12) на ошибки
2. Восстановите бэкап
3. Проверьте, не удалили ли важные поля

---

## 📞 Контакты для помощи

При возникновении проблем:
1. Проверьте, есть ли бэкап
2. Попробуйте восстановить из бэкапа
3. Обратитесь к разработчику с описанием проблемы

---

## 🎓 Примеры редактирования

### Пример 1: Изменить цену абонемента

**Было:**
```
8 занятий — 2600 ₽
```

**Стало:**
```
8 занятий — 2800 ₽
```

**Где**: Колонка "Полное описание", категория "gym"

### Пример 2: Добавить нового тренера

1. Найдите последнюю строку с тренером (например, gym_2_10)
2. Скопируйте строку
3. Измените ID на gym_2_11
4. Заполните данные нового тренера
5. Убедитесь, что "Родитель" = "gym/trainer"

### Пример 3: Обновить расписание

**Было:**
```
Пн, Ср, Пт 10:00-12:00
```

**Стало:**
```
Пн, Ср, Пт 10:00-12:00<br>Сб 14:00-16:00
```

**Где**: Колонка "Расписание"

---

## 📊 Чек-лист перед публикацией

- [ ] Экспортировали данные (`export-to-google.ps1`)
- [ ] Загрузили в Google Таблицы
- [ ] Отредактировали контент
- [ ] Скачали CSV обратно
- [ ] Импортировали на сайт (`import-from-google.ps1`)
- [ ] Проверили сайт локально (http://localhost:8000)
- [ ] Убедились, что все работает
- [ ] Опубликовали на сервере

---

## 🎉 Готово!

Теперь вы можете легко редактировать контент сайта через удобный интерфейс Google Таблиц!

**Удачи в работе!** 🚀

---

*Документация создана: 2025-11-05*
*Версия: 1.0*
*Автор: Claude Code*
